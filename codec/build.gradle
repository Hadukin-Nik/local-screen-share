plugins {
    id 'java'
}

dependencies {
    // Зависимость от модуля core для интерфейсов захвата
    implementation project(':core')

    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useJUnitPlatform()
}
